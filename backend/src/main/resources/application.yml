spring:
  application:
    name: borsa

  datasource:
    url: jdbc:postgresql://localhost:5432/borsa_db
    username: postgres
    password: 159753
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0

server:
  port: 8080

jwt:
  secret: your-super-secret-key-must-be-at-least-256-bits-long-for-hs256
  expiration: 86400000